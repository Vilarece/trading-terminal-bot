// Sept 17, 2026: the Actions API, trade page and Telegram bot now run as one
// process — see ../app.mjs. Kept so `npm run server` (Render's existing start
// command for clearlane-actions-api) still works unchanged.
import "../app.mjs";
